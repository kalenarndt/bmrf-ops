---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: coder
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: coder
      version: 0.22.0
      sourceRef:
        kind: HelmRepository
        name: coder
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: coder-values